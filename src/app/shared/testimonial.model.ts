export class Testimonial {

  constructor(public foto: string, public name: string, public job: string, public description: string, public date: string) {
    this.foto = foto
    this.name = name
    this.job = job
    this.description = description
    this.date = date
  }
}
